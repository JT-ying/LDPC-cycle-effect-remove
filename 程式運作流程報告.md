# LDPC 程式運作流程與主要函式功能分析報告

這是一份基於 LDPC 程式碼目錄與檔案架構，整理出的程式運作流程與主要函式功能分析報告：

### 1. 初始化與模擬設定 (Initialization & Configuration)
- **檔案/位置**：`main.cpp`、`Configuration.h`
- **功能摘要**：
  程式執行的起點。首先會根據設定決定使用的解碼演算法（例如 Sum-Product, Log-Sum-Product, MSA, NMSA 等）、是否要消除特定的短環效應（Cycle Effect, 如 4-cycle、6-cycle 等），以及設定亂數種子。
  接著程式會去讀取外部的 LDPC 檢查矩陣（H Matrix）資料檔（例如 `408.3.854.dat` 等），提取出其欄與列的資訊（`n`、`m`、`coldegree`），並配置給各個運算所需的陣列與記憶體空間。

### 2. 前置處理與矩陣轉換 (Matrix Preprocessing)
- **核心函式**：`Preprocess()` 與 `RCmatrix()`
- **檔案**：`Preprocess.cpp` 與 `main.cpp`
- **功能摘要**：
  - **`Preprocess()`**：透過高斯消去法處理原始的檢查矩陣 $H$，找出現實中具備獨立性的列數（去除相依列），並由此推導產生相對應的生成矩陣（Generator Matrix, $G$），以供稍後的編碼階段使用。
  - **`RCmatrix()`**：為了將後續解碼演算法的複雜度降到最低，它會掃描 H 矩陣以建立相鄰節點（Adjacency）清單。也就是建立 `R`（每列中有哪些行的元為 1）與 `C`（每行中有哪些列的元為 1）這兩張對照表，這對訊息傳遞演算法 (Message Passing) 至關重要。

### 3. 編碼階段 (Encoding)
- **核心函式**：`Encode()`
- **檔案**：`Encode.cpp`
- **功能摘要**：
  利用亂數隨機產生長度為 $k$（也就是 $n-m$）的二元使用者訊息 `message` {0, 1}。接著將訊息矩陣與生成矩陣 $G$ 相乘，產生出了一組長度為 $n$ 的合法編碼字元（codeword, $c$）。

### 4. 調變與通道雜訊模擬 (Modulation & Channel Noise)
- **核心函式**：`uniform()`、`gaussian()`
- **檔案**：`AWGN.cpp` 與主程式迴圈
- **功能摘要**：
  - **BPSK 調變**：主程式中會將發送的 codeword {$0, 1$} 映射成具有極性的傳輸訊號（例如 $+1, -1$），準備通過通道。
  - **模擬 AWGN**：為了模擬真實世界中傳輸所受到的高斯白雜訊（Additive White Gaussian Noise）干擾：
    - `uniform()` 負責產生界於 0 到 1 之間的均勻分佈隨機數。
    - `gaussian()` 利用中心極限定理，透過累積多個 `uniform()` 輸出來逼近常態分佈（Gaussian Distribution），進而產生具備特定平均值為 0，並根據目前的 SNR（訊噪比）得出特定變異數的雜訊值。
  這股雜訊被疊加在原本乾淨的訊號上，作為接收端觀測到的數值 $y$。

### 5. 通道軟性資訊與 LLR 計算 (LLR Calculation)
- **檔案**：主程式中
- **功能摘要**：
  接收到被雜訊污染的訊號 $y$ 後，程式先計算每一個位元的對數概似比（Log-Likelihood Ratio, LLR），將類比的訊號強度轉化成為機率軟資訊，送入後續的解碼器使用。

### 6. 解碼運算主體 (Decoding & Message Passing)
- **核心函式**：`sumproduct1()`, `sumproduct2()`, `sumproduct3()`, `logsumproduct()`, `logsumproduct_cycle()` 等。
- **檔案**：`Decode.cpp`, `decode_MSC.cpp`
- **功能摘要**：
  這是 LDPC 中核心的錯誤更正技術，程式採納了二分圖（Bipartite graph）的**訊息傳遞演算法 (Message Passing Algorithm)**。
  - 在每一次迭代中，變數節點 (Variable Nodes) 會先將訊息彙整，計算並送往檢查節點 (Check Nodes)，也就是更新 $q_{ij}$。
  - 接著由上述的函式如 `sumproduct` 進行檢查節點端的數值更新，並將訊息 $r_{ji}$ 回傳。
  - 程式針對「短環效應」(Short cycles) 設計了如 `logsumproduct_cycle` 或演算法內特別的旗標判斷，用以降低如果圖中出現 4-cycle 或 6-cycle 導致迭代訊息收斂不良的情況。
  這個過程會一直迭代，直到算出該 codeword 完全符合所有同位檢查方程式（Syndrome check 全部過關）或者是達到了程式定義的最高容許迭代次數為止。

### 7. 判決與統計紀錄 (Decision & Error Statistics)
- **功能摘要**：
  - 解碼迴圈結束後，程式會統合每個位元收到的機率，進行「硬判決 (Hard Decoding)」，如果得出的數值為正則判讀為 0，為負則判讀為 1，藉此得到最終的解碼序列。
  - 將解碼結果與一開始傳送的 `message` / `codeword` 對比。統計 **BER (Bit Error Rate, 誤碼率)** 與其他錯誤區塊次數。
  - 最後將每一次測試的結果與各個 SNR 底下的數據輸出至設定的 `.dat` 或 `sim_log.txt` 中，結束分析。
